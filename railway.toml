[build]
builder = "nixpacks"

[deploy]
# This runs once before the app starts. If it fails, the old version stays live.
releaseCommand = "python manage.py migrate"

# This only runs the web server
startCommand = "gunicorn rehub.wsgi:application --workers 1 --threads 2 --timeout 120"